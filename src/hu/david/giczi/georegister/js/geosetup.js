var msg = document.getElementById("msg").value;


if (msg === "invalid") {

	alert("Mind a négy beállítás megadása szükséges.")
} else if (msg === "setup") {

	alert("A program megfelelő működéséhez hozza létre \"C:\\geoprops\" nevű mappát, és adja meg a szükséges beállításokat.")
}